# -------------------------------------------------------
# OCR Evaluation — requirements.txt
# Contrainte principale : PaddleOCR requiert numpy < 2.0
# -------------------------------------------------------

# Numpy — version bloquée < 2.0 pour compatibilité PaddleOCR
numpy>=1.24.0,<2.0.0

# PDF → image
PyMuPDF>=1.23.0,<1.25.0          # fitz ; 1.25+ abandonne le support numpy <2 indirect

# Traitement image
Pillow>=10.0.0,<11.0.0            # numpy <2 testé jusqu'à Pillow 10.x
opencv-python>=4.8.0,<4.10.0     # 4.10+ peut exiger numpy>=2 selon platform

# OCR Tesseract
pytesseract>=0.3.10

# OCR PaddleOCR
paddleocr>=2.7.0,<3.0.0           # branche 2.x stable avec numpy <2.0
paddlepaddle>=2.5.0,<2.7.0        # backend CPU ; remplacer par paddlepaddle-gpu si GPU

# LLM local (inference GGUF via llama.cpp)
# !! NE PAS installer via pip install -r sur Windows (chemins trop longs + pas de compilateur C++)
# Installer séparément avec la roue pré-compilée :
#
#   CPU  : pip install llama-cpp-python --only-binary=:all: --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
#   CUDA : pip install llama-cpp-python --only-binary=:all: --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121

# Index vectoriel FAISS
faiss-cpu==1.7.4                  # 1.8.0+ peut résoudre numpy>=2 ; 1.7.4 sans contrainte numpy

# Embeddings RAG
sentence-transformers==2.7.0      # dernière version branche 2.x, sans contrainte numpy>=2
torch>=1.11.0,<2.3.0              # 2.3+ peut tirer numpy>=2 ; 2.2.x est la cible sûre
transformers>=4.34.0,<5.0.0       # requis par sentence-transformers
huggingface-hub>=0.15.1,<1.0.0    # idem
scikit-learn>=1.3.0,<1.5.0        # requis par sentence-transformers, compatible numpy <2
scipy>=1.10.0,<1.13.0             # idem
